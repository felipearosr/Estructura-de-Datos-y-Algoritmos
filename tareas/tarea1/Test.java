import java.util.*;

public class Test {
    public static void main(String[] args) {
        Random random = new Random();
        int min = 10000,
            max = 1000000;
        int[] arr = random.ints(Integer.MIN_VALUE,
        nextInt(min-max) + min, Integer.MIN_VALUE).toArray();
    }
}